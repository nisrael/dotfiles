---
# Ubuntu installation via Microsoft repository
- name: Add Microsoft GPG key on Ubuntu
  ansible.builtin.get_url:
    url: https://packages.microsoft.com/keys/microsoft.asc
    dest: /etc/apt/keyrings/packages.microsoft.asc
    mode: '0644'
  become: true
  when: ansible_distribution == "Ubuntu"

- name: Add VSCode APT repository on Ubuntu
  ansible.builtin.apt_repository:
    repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/packages.microsoft.asc] https://packages.microsoft.com/repos/code stable main"
    filename: vscode
    state: present
  become: true
  when: ansible_distribution == "Ubuntu"

- name: Install Visual Studio Code on Ubuntu
  ansible.builtin.apt:
    name: code
    state: present
    update_cache: true
  become: true
  when: ansible_distribution == "Ubuntu"

# Fedora installation via Microsoft repository
- name: Add Microsoft GPG key on Fedora
  ansible.builtin.rpm_key:
    key: https://packages.microsoft.com/keys/microsoft.asc
    state: present
  become: true
  when: ansible_distribution == "Fedora"

- name: Add VSCode repository on Fedora
  ansible.builtin.yum_repository:
    name: vscode
    description: Visual Studio Code
    baseurl: https://packages.microsoft.com/yumrepos/vscode
    enabled: true
    gpgcheck: true
    gpgkey: https://packages.microsoft.com/keys/microsoft.asc
  become: true
  when: ansible_distribution == "Fedora"

- name: Install Visual Studio Code on Fedora
  ansible.builtin.dnf:
    name: code
    state: present
  become: true
  when: ansible_distribution == "Fedora"

# macOS installation via Homebrew
- name: Install Visual Studio Code on macOS
  community.general.homebrew_cask:
    name: visual-studio-code
    state: present
  when: ansible_distribution == "MacOSX"

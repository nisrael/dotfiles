# Alacritty configuration
# Generated by Ansible - DO NOT EDIT MANUALLY
# Platform: {{ ansible_facts.distribution }}{% if ansible_facts.kernel is search("microsoft") %} (WSL){% endif %}

[general]
import = [
{% if ansible_facts.distribution == "MacOSX" %}
    # macOS-specific settings (includes common.toml)
    "~/.config/alacritty/macos.toml",
{% elif ansible_facts.kernel is search("microsoft") %}
    # WSL-specific settings (includes common.toml)
    "~/.config/alacritty/windows.toml",
{% elif ansible_facts.system == "Linux" %}
    # Native Linux-specific settings (includes common.toml)
    "~/.config/alacritty/linux.toml",
{% endif %}
]

# Local machine-specific overrides can be added below
# Or create ~/.config/alacritty/alacritty.local.toml and import it here

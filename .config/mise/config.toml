# mise configuration
# Global tools installed across all projects

[tools]
# Node.js LTS (use 'lts' for latest LTS, or specific version like '22.11.0')
node = "lts"

# pnpm package manager
pnpm = "latest"

[settings]
# Automatically install tools when entering directories with .tool-versions or .mise.toml
auto_install = true

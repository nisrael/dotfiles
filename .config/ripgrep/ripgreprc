# ripgrep configuration
# Uses RIPGREP_CONFIG_PATH environment variable

# Search hidden files and directories
--hidden

# Follow symbolic links
--follow

# Don't respect .gitignore files
# Uncomment if you want to search gitignored files:
# --no-ignore

# Ignore specific directories
--glob=!.git/*
--glob=!node_modules/*
--glob=!target/*
--glob=!build/*
--glob=!dist/*
--glob=!.next/*
--glob=!.cache/*
--glob=!*.min.js
--glob=!*.min.css

# Smart case: case-insensitive if all lowercase, case-sensitive otherwise
--smart-case

# Show line numbers
--line-number

# Use colors
--colors=line:fg:yellow
--colors=line:style:bold
--colors=path:fg:green
--colors=path:style:bold
--colors=match:fg:red
--colors=match:style:bold

# Max columns for preview (prevents super long lines)
--max-columns=150
--max-columns-preview
